# Uncomment the next line to define a global platform for your project
 platform :ios, '13.0'

target 'AppMeli' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppMeli
  pod 'Alamofire', '~> 5.4'
  pod 'SwiftyJSON'

  target 'AppMeliTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppMeliUITests' do
    # Pods for testing
  end

end
